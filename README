This project is dead, left on github in case any CSHers need to look at a non Node JS or Erlang approach would look like.

Make sure libmysql++-dev, libboost-thread-dev, and libboost-regex-dev are installed.
